'use strict';

var pipeline = require('../src/index.js');
var expect = require('chai').expect;

describe('webui-pipeline-serve-', function () {
  it('should exist', function () {
    expect(pipeline).to.exist;
  });
});
