'use strict';

 /* describe('Dra Details', function () {
  var $compile;
  var element;
  var scope;

  var mockTemplates = {
    basic: '<ne-dra-details></ne-dra-details>'
  };

  beforeEach(module('ne.components.DraDetailsModule'));

  beforeEach(module('modules/components/dra-details/templates/dra-details.html'));

  function compileDirective(params) {
    var element = angular.element(params.template);
    $compile(element)(params.scope);
    params.scope.$apply();
    return element;
  }

  beforeEach(inject(function (_$compile_, _$rootScope_) {
    $compile = _$compile_;
    scope = _$rootScope_.$new();
  }));

  describe('When including the directive', function () {

    beforeEach(function () {
      element = compileDirective({
        scope: scope,
        template: mockTemplates.basic
      });
    }); */
